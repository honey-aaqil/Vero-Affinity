# Copy this file to `.env.local` and fill in the values.
# Next.js automatically loads `.env.local` in development.

# MongoDB
# Example local URI: mongodb://localhost:27017
# Example Atlas URI: mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority
MONGODB_URI=

# The database name to use within the MongoDB cluster.
MONGODB_DB=

# App secrets
# Used to sign/verify sessions or other server-side tokens.
SESSION_SECRET=

# --- Future / Phase 2+ (optional for now) ---
# Used as an input to encryption key derivation for end-to-end / at-rest encryption.
ENCRYPTION_SALT=

# GridFS bucket prefix (collections will be `<bucket>.files` and `<bucket>.chunks`).
# The default bucket used by this repo is `media`.
GRIDFS_MEDIA_BUCKET=media
